import { NoGamesWrapper } from "./styles";

/**
 *
 * @returns {JSX.Element}
 */

const NoGamesContainer = (): JSX.Element => {
  return <NoGamesWrapper>There's no selected game id.</NoGamesWrapper>;
};

export default NoGamesContainer;
